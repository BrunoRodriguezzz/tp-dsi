spring.application.name=serviceRegistry
server.port=8088

# Configuración del cliente Eureka (servidor no se registra a sí mismo)
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Configuración de Actuator
eureka.instance.health-check-url-path=/actuator/health
eureka.instance.status-page-url-path=/actuator/info

# Configuraciones para un mejor manejo de servicios en contenedores
eureka.server.enable-self-preservation=true
eureka.server.eviction-interval-timer-in-ms=15000
eureka.server.renewal-percent-threshold=0.49

# Configuración de URLs y zona por defecto
eureka.client.serviceUrl.defaultZone=http://localhost:8088/eureka/
eureka.server.peer-node-read-timeout-ms=1000
eureka.server.wait-time-in-ms-when-sync-empty=0

# Configuración de management endpoints
management.endpoints.web.exposure.include=health,info,env,metrics
management.endpoint.health.show-details=always
management.server.port=8088

# Configuración de seguridad (deshabilitada para simplificar)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
spring.security.enabled=false

# CONFIGURACIONES COMENTADAS PARA PRODUCCIÓN:
# Para entornos de producción, descomentar estas configuraciones:

# Configuración para PRODUCCIÓN - habilitar self-preservation más estricto
#eureka.server.enable-self-preservation=true
#eureka.server.renewal-percent-threshold=0.85
#eureka.server.eviction-interval-timer-in-ms=60000
#eureka.server.wait-time-in-ms-when-sync-empty=5

# Configuración de logging para debugging (descommentar si es necesario)
#logging.level.com.netflix.eureka=DEBUG
#logging.level.com.netflix.discovery=DEBUG
